# TODO
- Consider making a fuzzy search for names of anything to cross reference to ids?
- ~~Update all dev files from Python template.~~
- Add a try/except to `icon()` endpoint to fetch a higher res icon by default.